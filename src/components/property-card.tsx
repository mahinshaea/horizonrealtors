import Link from 'next/link'; import { Bath, BedDouble, MapPin, Maximize } from 'lucide-react'; import type { Property } from '@/lib/types'; import { rent, titleCase } from '@/lib/utils'
export function PropertyCard({property}: {property:Property}){const cover=property.property_images?.find(i=>i.is_cover)||property.property_images?.[0];return <article className="overflow-hidden rounded-lg border border-stone-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className="relative h-52 bg-stone-200">{cover?.image_url?<img src={cover.image_url} alt={property.title} className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center bg-stone-200 font-serif text-stone-500">Horizon Realtors</div>}{property.featured&&<span className="absolute left-3 top-3 rounded bg-amber-500 px-2 py-1 text-xs font-bold text-white">Featured</span>}</div><div className="p-5"><p className="text-xs font-semibold uppercase tracking-wider text-emerald-700">{titleCase(property.property_type)} · {property.availability?'Available':'Unavailable'}</p><h3 className="mt-2 font-serif text-xl font-bold text-emerald-950">{property.title}</h3><p className="mt-2 flex items-center gap-1 text-sm text-stone-500"><MapPin size={15}/>{property.location}</p><p className="mt-4 text-lg font-bold text-emerald-900">{rent(property.monthly_rent)}</p><div className="mt-4 flex gap-3 border-t border-stone-100 pt-4 text-xs text-stone-600"><span className="flex items-center gap-1"><BedDouble size={14}/>{property.bedrooms} Bed</span><span className="flex items-center gap-1"><Bath size={14}/>{property.bathrooms} Bath</span><span className="flex items-center gap-1"><Maximize size={14}/>{property.area} {property.area_unit}</span></div><Link href={`/properties/${property.slug}`} className="mt-5 block rounded border border-emerald-800 py-2 text-center text-sm font-semibold text-emerald-800">View details</Link></div></article>}
