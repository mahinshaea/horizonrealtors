import { notFound } from 'next/navigation'; import { createClient } from '@/lib/supabase/server'; import { PropertyForm } from '@/components/property-form'; export default async function Edit({params}:{params:Promise<{id:string}>}){const s=await createClient();const {data}=await s.from('properties').select('*, property_images(*)').eq('id',(await params).id).single();if(!data)notFound();return <div className="p-6 md:p-10"><h1 className="font-serif text-3xl font-bold text-emerald-950">Edit property</h1><div className="mt-8"><PropertyForm property={data}/></div></div>}
