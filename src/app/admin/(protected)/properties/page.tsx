import Link from 'next/link'; import { createClient } from '@/lib/supabase/server'; import { rent } from '@/lib/utils'; export default async function AdminProperties(){const s=await createClient();const {data:properties}=await s.from('properties').select('*').order('created_at',{ascending:false});return <div className="p-6 md:p-10"><div className="flex items-center justify-between"><div><p className="text-sm text-stone-500">Inventory</p><h1 className="font-serif text-3xl font-bold text-emerald-950">Properties</h1></div><Link href="/admin/properties/new" className="rounded bg-emerald-800 px-4 py-2 text-sm font-bold text-white">Add property</Link></div><div className="mt-8 overflow-x-auto rounded-lg bg-white shadow-sm"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b text-stone-500"><tr><th className="p-4">Property</th><th>Location</th><th>Rent</th><th>Status</th><th></th></tr></thead><tbody>{properties?.map(p=><tr key={p.id} className="border-b last:border-0"><td className="p-4 font-semibold">{p.title}</td><td>{p.location}</td><td>{rent(p.monthly_rent)}</td><td><span className="rounded bg-stone-100 px-2 py-1 text-xs">{p.availability?'Available':'Unavailable'}</span></td><td><Link href={`/admin/properties/${p.id}/edit`} className="font-semibold text-emerald-800">Edit</Link></td></tr>)}</tbody></table></div></div>}
