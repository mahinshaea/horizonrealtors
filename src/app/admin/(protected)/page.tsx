import Link from 'next/link'; import { createClient } from '@/lib/supabase/server'; export default async function Admin(){const s=await createClient();const [{count:total},{count:available},{count:unavailable},{count:featured},{count:enquiries},{data:recentProperties},{data:recentEnquiries}]=await Promise.all([s.from('properties').select('*',{count:'exact',head:true}),s.from('properties').select('*',{count:'exact',head:true}).eq('availability',true),s.from('properties').select('*',{count:'exact',head:true}).eq('availability',false),s.from('properties').select('*',{count:'exact',head:true}).eq('featured',true),s.from('enquiries').select('*',{count:'exact',head:true}),s.from('properties').select('id,title,location,created_at').order('created_at',{ascending:false}).limit(5),s.from('enquiries').select('id,name,status,created_at,properties(title)').order('created_at',{ascending:false}).limit(5)]);const stats=[['Total properties',total],['Available',available],['Unavailable',unavailable],['Featured',featured],['Enquiries',enquiries]];return <div className="p-6 md:p-10"><div className="flex items-center justify-between"><div><p className="text-sm text-stone-500">Operations overview</p><h1 className="font-serif text-3xl font-bold text-emerald-950">Dashboard</h1></div><Link href="/admin/properties/new" className="rounded bg-emerald-800 px-4 py-2 text-sm font-bold text-white">Add property</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{stats.map(([label,count])=><div key={label as string} className="rounded-lg bg-white p-5 shadow-sm"><p className="text-sm text-stone-500">{label as string}</p><p className="mt-2 text-3xl font-bold text-emerald-950">{count as number||0}</p></div>)}</div><div className="mt-10 grid gap-6 lg:grid-cols-2"><section className="rounded-lg bg-white p-6 shadow-sm"><h2 className="font-serif text-xl font-bold text-emerald-950">Recent properties</h2><div className="mt-4 grid gap-3">{recentProperties?.map(p=><Link href={`/admin/properties/${p.id}/edit`} key={p.id} className="flex justify-between border-b pb-3 text-sm"><span>{p.title}</span><span className="text-stone-500">{p.location}</span></Link>)}</div></section><section className="rounded-lg bg-white p-6 shadow-sm"><h2 className="font-serif text-xl font-bold text-emerald-950">Recent enquiries</h2><div className="mt-4 grid gap-3">{recentEnquiries?.map(e=><Link href="/admin/enquiries" key={e.id} className="flex justify-between border-b pb-3 text-sm"><span>{e.name}</span><span className="text-stone-500">{e.status}</span></Link>)}</div></section></div></div>}
